import { FirstCharacterUpperCasePipe } from './first-character-upper-case.pipe';

describe('FirstCharacterUpperCasePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstCharacterUpperCasePipe();
    expect(pipe).toBeTruthy();
  });
});
